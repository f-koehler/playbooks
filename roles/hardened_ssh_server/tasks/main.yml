- name: copy ssh server config
  copy:
    src: sshd_config
    dest: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: "600"
  notify:
    - restart ssh server

- name: open ports for ssh server
  ufw:
    rule: allow
    port: "{{ item | int }}"
  loop:
    - 20257
