---
- name: Generate SSH key
  community.crypto.openssh_keypair:
    comment: "{{ username }}@{{ hostname }}.{{ domain }}"
    group: "{{ groupname }}"
    owner: "{{ username }}"
    mode: "400"
    path: "~/.ssh/id_ed25519"
    type: ed25519
