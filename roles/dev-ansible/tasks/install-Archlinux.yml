---
- name: Install ansible development tools
  become: true
  community.general.pacman:
    state: present
    name:
      - ansible
      - ansible-lint
      - ansible-language-server
      - yamllint
