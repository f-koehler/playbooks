- name: install docker
  pacman:
    name: docker,docker-compose
    state: present
  become: yes
  notify:
    - restart docker

- name: enable and start docker
  service:
    name: docker
    enabled: yes
    state: started
  become: yes
