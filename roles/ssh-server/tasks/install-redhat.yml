---
- name: "DNF | Install openssh-server"
  become: true
  ansible.builtin.dnf:
    name: openssh-server
    state: present

- name: Set openssh service name
  ansible.builtin.set_fact:
    ssh_server_service_name: sshd
