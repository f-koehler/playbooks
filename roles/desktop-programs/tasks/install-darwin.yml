---
- name: "Homebrew | Install common desktop programs (Cask)"
  community.general.homebrew_cask:
    state: present
    sudo_password: "{{ ansible_become_password }}"
    name:
      - drawio
      - firefox
      - gimp
      - inkscape
      - nextcloud
      - obsidian
      - libreoffice

- name: "Homebrew | Install common desktop programs"
  community.general.homebrew:
    state: present
    name:
      - mpv
