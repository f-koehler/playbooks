- hosts: webservers
  become: yes
  vars:
    timezone: "Europe/Berlin"
  roles:
    - vps_kernel
    - firewall
    - hardened_ssh_server
    - localtime
    - haveged
    - docker
    - vps_containers
