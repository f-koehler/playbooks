- hosts: webservers
  become: yes
  vars:
    timezone: "Europe/Berlin"
  vars_files:
    - secrets/vps.yml
  roles:
    - vps-kernel
    - firewall
    - hardened-ssh-server
    - localtime
    - haveged
    - docker
    - vps-containers
